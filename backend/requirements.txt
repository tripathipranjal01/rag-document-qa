# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
openai==1.3.7
python-dotenv==1.0.0

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0
tiktoken==0.5.1

# Basic image processing (lighter version)
Pillow==10.0.1
pytesseract==0.3.10

# HTTP client
httpx==0.25.2

# Note: Removed heavy packages to fix build timeout:
# - numpy (can cause build issues)
# - easyocr (very heavy, includes PyTorch)
# - setuptools/wheel (usually not needed)
# OCR will use only Tesseract for now